<%= form_for @item_price do |f| %>
  <% if @item_price.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@item_price.errors.count, "error") %> prohibited this item from being saved:</h2>

      <ul>
      <% @item_price.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <fieldset>

  <div class="field">
    <%= f.label :item_price_item_id %><br />
    <%= f.collection_select :item_id, Item.active.all, :id, :name, prompt: "Select..." %>
  </div>
  <div class="field">
    <%= f.label :item_price_price %><br />
    <%= f.text_field :price %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>

  </fieldset>
<% end %>