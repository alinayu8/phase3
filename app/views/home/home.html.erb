<br><h3 class="center-title">Reorder List</h3><br>
<table class="center-datagrid">
    <thead class="head-datagrid">
        <th>Item</th>
        <th>Inventory</th>
        <th>Reorder Level</th>
    </thead>
<% for item in @items_to_reorder %>
    <tr class="<%= cycle('dg_list_line_odd', 'dg_list_line_even') %>">
    <td><%= item.name %></td>
    <td><%= item.inventory_level %></td>
    <td><%= item.reorder_level %></td>
    </tr>
<% end %>
</table>